/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "bqgrievuy3qajnep.public.blob.vercel-storage.com",
      },
    ],
  },
};

export default nextConfig;
